{
  "name": "weather-forecasting-app-mern",
  "private": true,
  "version": "1.0.0",
  "description": "Submission-ready MERN weather forecasting app.",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install",
    "dev": "start \"server\" cmd /c \"cd server && npm run dev\" && start \"client\" cmd /c \"cd client && npm run dev\"",
    "server": "npm --prefix server run dev",
    "client": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start"
  }
}
